(re-seq #"\w" "AEIOULNRST")
(load-file "etl_test.clj")
(def sam {1 ["WORLD"]})
(def sam {1 ["WORLD" "WAR"]})
(map #(vec (first %) (second %)) sam)
(map #(vector (first %) (second %)) sam)
(map #(vector (second %) (first %)) sam)
(map #(interleave (second %) (repeatedly (first %))) sam)
(map #(interleave (second %) (constantly (first %))) sam)
(zipmap ["foo" "bar"] (repeatedly 1))
(zipmap ["foo" "bar"] (repeat 1))
(map #(interleave (second %) (repeat (first %))) sam)
(def sam {1 ["WORLD" "WAR"] 2 ["boat" "ballerina"]})
(map #(interleave (second %) (repeat (first %))) sam)
(reduce merge (map #(interleave (second %) (repeat (first %))) sam))
(reduce into {} (map #(interleave (second %) (repeat (first %))) sam))
(flatten (map #(interleave (second %) (repeat (first %))) sam))
(into {} (flatten (map #(interleave (second %) (repeat (first %))) sam)))
(apply hash-map (flatten (map #(interleave (second %) (repeat (first %))) sam)))
(load-file "etl_test.clj")
